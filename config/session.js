/**
 * Created by maocg.
 * Date:2016/3/12 0012.
 * Time:下午 10:20.
 */
/*
var Topic = require('../dao/topicDao'),
    User = require('../dao/userDao'),
    Reply = require('../dao/replyDao');
var session = {};

exports.getTopicMap = function (session) {
    Topic.getAllTopic(session,function(err,data){
        if(err)
           return next(err);
       if(data.length>0){
           for(var p in data){
               session[p._id]=
           }
       }
    });
}*/
